apiVersion: 1

providers:
  - name: idempotency-service
    orgId: 1
    folder: Idempotency Service
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
