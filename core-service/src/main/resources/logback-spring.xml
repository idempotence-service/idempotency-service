<configuration>
    <springProperty scope="context" name="applicationName" source="spring.application.name" defaultValue="core-service"/>
    <springProperty scope="context" name="instanceId" source="app.instance-id" defaultValue="core-service"/>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd'T'HH:mm:ss.SSSXXX} %-5level [%thread] %logger{36} app=${applicationName} instance=${instanceId} globalKey=%X{globalKey:-} uid=%X{uid:-} integration=%X{integration:-} owner=%X{ownerId:-} - %msg%n%ex</pattern>
        </encoder>
    </appender>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>
</configuration>
